# .github/workflows/example-2.yml

on:
    pull_request:
    push:
        branches:
            - master

jobs:
    bump-version:
        name: Bump Package Version
        runs-on: ubuntu-latest
        steps:
            - run: touch lol.txt
            - name: upload android apks
              uses: actions/upload-artifact@v2
              with:
                  name: "android-test-apks-${{ env.GITHUB_SHA }}"
                  path: ./lol.txt
                  retention-days: 1
                  env: 
                    GITHUB_SHA: ${GITHUB_SHA}
